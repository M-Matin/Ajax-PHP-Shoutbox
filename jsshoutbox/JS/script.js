$(document).ready(function(){
//eventhandler for what happend when submit btn is clicked
  $('#submit').on('click', function(){
//define variable for each form value we are going to use
    var name = $('#name'),val();
    var shout = $('#shout').val();
//getdate() function is what we are going to define later to show the time of user input
    var date = getdate();
  });
});
